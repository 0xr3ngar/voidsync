import type { Platform } from "@/models/Platform";

export const handleSoundCloudConnect = (
    platform: Platform,
    onConnect: (isConnected: boolean) => void,
) => {
    // TODO: Implement SoundCloud connection
};
