import type { Platform } from "@/models/Platform";

export const handleSpotifyConnect = (
    platform: Platform,
    onConnect: (isConnected: boolean) => void,
) => {
    // TODO: Implement Spotify connection
};
