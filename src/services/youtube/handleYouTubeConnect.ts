import type { Platform } from "@/models/Platform";

export const handleYouTubeConnect = (
    platform: Platform,
    onConnect: (isConnected: boolean) => void,
) => {
    // TODO: Implement YouTube connection
};
